# ChatLog

Internal product repository. Deployment targets are configured per environment; use the provided configuration files when connecting hosting providers.

Do not commit secrets. Copy `.env.example` for local and hosted environment variables.
