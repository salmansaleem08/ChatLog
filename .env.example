# Copy to `.env.local` in `web/`, and optionally `whatsapp-service/.env`.
# Never commit real secrets.

# Transactional email
RESEND_API_KEY=

# AI extraction (minimize calls in production; persist structured data in DB)
GEMINI_API_KEY=
OPENROUTER_API_KEY=

# Supabase (web app + server-side)
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=
SUPABASE_SERVICE_ROLE_KEY=

# Python service (Render injects PORT)
# PORT is set automatically on Render
